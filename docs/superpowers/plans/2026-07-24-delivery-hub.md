# Delivery Hub Implementation Plan

**Goal:** Redesign project detail as delivery hub; reshape portal into review hub C.

## Tasks

1. Schema + types + templates + store/db  
2. Seed phases on createProject; actions for checklist/phase/approvals/runbook  
3. Rebuild `project-detail.tsx` layout  
4. Rebuild portal client UI as review hub  
5. Slim Portal tab to access settings only  
6. `npm run build`
