# AI Quotes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Persist simple AI-assisted quotes (intro + scope + line items), list/edit them, Mark sent with `P-YY-NNNN`, and download a proposal-style PDF (not invoice-looking).

**Architecture:** Supabase `quotes` + firm quote sequence. Server actions for CRUD / AI generate / mark sent. Separate pdf-lib renderer. UI mirrors invoices lightly but with discovery notes + Generate. Anthropic JSON draft like email/qualify.

**Tech Stack:** Next.js App Router, Supabase, Anthropic, pdf-lib, existing store/db patterns from invoices.

**Spec:** `docs/superpowers/specs/2026-07-26-ai-quotes-design.md`

## Global Constraints

- Quote PDF must **not** include IBAN, due date, payable, or payment block
- Line items v1: `{ description, amount }` only; currency EUR
- Locale `sl` | `en` chosen by user at generate
- Number `P-YY-NNNN` only on Mark sent
- No Resend PDF send, no convert→invoice in v1
- Do not commit unless the user explicitly asks
- Verify with `npx tsc --noEmit`
- Read Next.js docs under `node_modules/next/dist/docs/` before new App Router APIs

## File map

| Path | Responsibility |
|------|----------------|
| `supabase/migrations/20260726140000_quotes.sql` | `quotes` table + `quote_next_sequence_by_year` on firm_settings |
| `src/lib/data.ts` | Quote types, normalize, totals helper |
| `src/lib/supabase/db.ts` | map/get/save quotes; firm sequence field |
| `src/lib/store.ts` | getQuotes / saveQuotes / getQuote |
| `src/lib/quotes/actions.ts` | CRUD, markSent, setStatus |
| `src/lib/quotes/ai.ts` | Anthropic generate draft |
| `src/lib/quotes/pdf.ts` | Proposal PDF bytes |
| `src/app/api/quotes/[id]/pdf/route.ts` | PDF download |
| `src/components/quote-editor.tsx` | Editor + AI generate UI |
| `src/components/quotes-table.tsx` | List |
| `src/app/quotes/page.tsx` | List page |
| `src/app/quotes/new/page.tsx` | New (optional leadId) |
| `src/app/quotes/[id]/page.tsx` | Detail |
| `src/app/quotes/[id]/edit/page.tsx` | Edit |
| `src/components/app-shell.tsx` | Nav |
| `src/components/command-palette.tsx` | Commands |
| `src/components/lead-detail.tsx` | New quote CTA |

---

### Task 1: Migration + types + store/db

**Files:**
- Create: `supabase/migrations/20260726140000_quotes.sql`
- Modify: `src/lib/data.ts` (Quote types after Invoices section)
- Modify: `src/lib/supabase/db.ts`
- Modify: `src/lib/store.ts`

**Interfaces:**
- Produces: `Quote`, `QuoteStatus`, `QuoteLineItem`, `normalizeQuote`, `computeQuoteTotals`, `getQuotes`, `saveQuotes`, `getQuote`

- [x] **Step 1: Migration**

```sql
-- supabase/migrations/20260726140000_quotes.sql
alter table public.firm_settings
  add column if not exists quote_next_sequence_by_year jsonb not null default '{}'::jsonb;

create table if not exists public.quotes (
  id text primary key,
  lead_id text references public.leads (id) on delete set null,
  status text not null default 'draft'
    check (status in ('draft', 'sent', 'accepted', 'declined')),
  locale text not null default 'sl'
    check (locale in ('sl', 'en')),
  number text,
  year integer,
  sequence integer,
  client_name text not null default '',
  client_company text not null default '',
  client_email text not null default '',
  intro text not null default '',
  scope text not null default '',
  notes text not null default '',
  discovery_notes text not null default '',
  line_items jsonb not null default '[]'::jsonb,
  currency text not null default 'EUR',
  subtotal numeric not null default 0,
  total numeric not null default 0,
  valid_until date,
  sent_at timestamptz,
  created_by text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

create unique index if not exists quotes_number_uidx
  on public.quotes (number) where number is not null;
create index if not exists quotes_status_idx on public.quotes (status);
create index if not exists quotes_lead_idx on public.quotes (lead_id);

alter table public.quotes enable row level security;
create policy "quotes all" on public.quotes
  for all to authenticated using (true) with check (true);
```

- [ ] **Step 2: Types in `data.ts`**

Add:

```ts
export const quoteStatuses = ["draft", "sent", "accepted", "declined"] as const;
export type QuoteStatus = (typeof quoteStatuses)[number];

export type QuoteLineItem = {
  description: string;
  amount: number;
};

export type Quote = {
  id: string;
  leadId: string | null;
  status: QuoteStatus;
  locale: "sl" | "en";
  number: string | null;
  year: number | null;
  sequence: number | null;
  clientName: string;
  clientCompany: string;
  clientEmail: string;
  intro: string;
  scope: string;
  notes: string;
  discoveryNotes: string;
  lineItems: QuoteLineItem[];
  currency: "EUR";
  subtotal: number;
  total: number;
  validUntil: string | null;
  sentAt: string | null;
  createdBy: string | null;
  createdAt: string;
  updatedAt: string;
};

export function computeQuoteTotals(items: QuoteLineItem[]) {
  const subtotal = items.reduce(
    (s, i) => s + (Number.isFinite(i.amount) ? i.amount : 0),
    0
  );
  return { subtotal, total: subtotal };
}

export function normalizeQuote(q: Quote): Quote { /* defaults + totals */ }
```

Also add `quoteNextSequenceByYear: Record<string, number>` to `FirmSettings` + defaults + normalize + db map/save (mirror invoice sequence).

- [ ] **Step 3: db + store**

Mirror invoices: `mapQuote`, `getQuotes`, `saveQuotes` (upsert/delete diff), `getQuote` via store find.

- [ ] **Step 4: Typecheck**

Run: `npx tsc --noEmit`  
Expected: PASS for new types/db.

---

### Task 2: CRUD + Mark sent actions

**Files:**
- Create: `src/lib/quotes/actions.ts`

**Interfaces:**
- Consumes: store quotes, firm settings, leads, activities
- Produces: `createQuote`, `updateQuote`, `deleteQuote`, `markQuoteSent`, `setQuoteStatus`

- [ ] **Step 1: Implement actions**

```ts
"use server";
// createQuote({ leadId?, clientName, ... }) → id
// updateQuote(id, patch) — recompute totals from lineItems
// deleteQuote(id)
// markQuoteSent(id):
//   - load firmSettings.quoteNextSequenceByYear
//   - year = current year; seq = (map[year] ?? 0) + 1
//   - number = `P-${String(year).slice(2)}-${String(seq).padStart(4,"0")}`
//   - status sent, sentAt now; bump firm sequence; save
//   - if leadId: activity type "proposal", title `Quote ${number}`; if lead status is before "Proposal sent", setLeadStatus to "Proposal sent"
// setQuoteStatus(id, accepted|declined|draft) — draft only if was sent? allow accepted/declined from sent
```

Revalidate `/quotes`, `/quotes/[id]`, lead paths.

- [ ] **Step 2: Typecheck**

Run: `npx tsc --noEmit`

---

### Task 3: AI generate

**Files:**
- Create: `src/lib/quotes/ai.ts`
- Modify: `src/lib/quotes/actions.ts` — `generateQuoteDraftAction`

**Interfaces:**
- Produces: `generateQuoteDraft({ leadId?, discoveryNotes, locale, lineHints?, existing? }) → { intro, scope, notes, lineItems, validUntil }`

- [ ] **Step 1: AI module**

Use Anthropic like `src/lib/ai/email.ts`:
- System: Tim Blažič / timblazic.dev, web + SEO quotes, honest scope, locale-locked output
- User: lead fields + discovery notes + optional amount hints
- Parse JSON; clamp amounts; require non-empty intro/scope

- [ ] **Step 2: Server action** wraps AI; does not auto-save (return draft for editor) OR saves into quote if `quoteId` passed — prefer: `generateQuoteDraftAction` returns JSON; editor applies then user saves.

- [ ] **Step 3: Typecheck**

---

### Task 4: Proposal PDF + route

**Files:**
- Create: `src/lib/quotes/pdf.ts`
- Create: `src/app/api/quotes/[id]/pdf/route.ts`

**Interfaces:**
- Produces: `buildQuotePdf(quote, issuer): Promise<Uint8Array>`

- [ ] **Step 1: PDF layout**

pdf-lib + fontkit (reuse invoice font loading pattern from `src/lib/invoices/pdf.ts` if shared helpers exist; do **not** copy payment section).

Sections top→bottom:
1. Issuer name + site
2. Title PONUDBA / QUOTE + number + dates
3. Recipient
4. Intro (wrapped)
5. Scope heading + wrapped text
6. Line table (description | amount)
7. Total
8. Notes

Labels by `quote.locale`.

- [ ] **Step 2: API route**

Studio session required; `GET` returns `application/pdf` attachment `quote-{number|id}.pdf`.

- [ ] **Step 3: Typecheck**

---

### Task 5: UI — list, editor, pages, nav

**Files:**
- Create components + app routes listed in file map
- Modify: `app-shell`, `command-palette`, `lead-detail`

**Interfaces:**
- Editor calls `createQuote` / `updateQuote` / `generateQuoteDraftAction` / link to PDF / `markQuoteSent`

- [ ] **Step 1: `quotes-table.tsx` + `/quotes` page**

Columns: number/draft, client, total, status, valid until, updated.

- [ ] **Step 2: `quote-editor.tsx`**

Fields: locale toggle, discovery notes, Generate button, intro textarea, scope textarea, line items (add/remove description+amount), notes, valid until. Save / Save & mark sent / Download PDF (if saved).

- [ ] **Step 3: Pages** `new` (read `leadId` searchParam, prefill from lead), `[id]`, `[id]/edit`

- [ ] **Step 4: Nav + palette + lead CTA** “New quote” → `/quotes/new?leadId=`

- [ ] **Step 5: Typecheck + manual smoke**

1. Run migration `20260726140000_quotes.sql`
2. From lead: New quote → notes → Generate SL → edit prices → Save → PDF looks like proposal
3. Mark sent → number `P-26-….` + lead activity / Proposal sent
4. Open in mail untouched; invoice PDF unchanged

---

## Spec coverage

| Spec | Task |
|------|------|
| quotes table + sequence | 1 |
| CRUD / statuses / mark sent | 2 |
| AI draft SL/EN | 3 |
| Non-invoice PDF | 4 |
| Nav / lead CTA / editor | 5 |
| No email send / no invoice convert | N/A (omit) |

## Consistency

- Number format `P-YY-NNNN` (2-digit year)
- Status union matches migration check
- Line item shape `{ description, amount }` everywhere
